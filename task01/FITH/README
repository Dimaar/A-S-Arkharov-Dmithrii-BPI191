Программа предоставляет собой структуру из двух разных выполняемых программ. Первая создаёт библиотеку, без которой вторая не может корректно выполнится, но если библиотека создана, то выполняется всё корректно.
